#the following variables must be set upon login,
#not when invoking shells, therefore not in .shinit
export EMACS_SOCKET_NAME=$XDG_RUNTIME_DIR/emacs/server

if [ -f ~/.bashrc ]; then
    source ~/.bashrc
fi
